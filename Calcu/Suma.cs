using System;
using System.Collections.Generic;
using System.Text;

namespace Calcu
{
    public class Suma
    {
        public static int Sum(int n1, int n2)
        {
            return n1 + n2;
        }

        /*
         * TestSumaF()
{ int [] 1 = {-2 2};
        try{
        Assert.Fail(Suma.Sum(1).ToString());
        } catch(InvalidOperationException e)
{}        */ 

    }
}
